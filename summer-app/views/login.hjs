<!DOCTYPE html>
<html>
  <head>
    <title>Challange Authentication</title>
    <link rel='stylesheet' href='/bower_components/font-awesome/css/font-awesome.css' />
    <link rel='stylesheet' href='/bower_components/bootstrap/dist/css/bootstrap.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  <div class="container">
	  <div class="jumbotron" style="margin-left:120px;width:1000px;background:none">

	  	<!-- Show any message that come back with authentication, returned message from server side '/routes/auth.js'
	  	{{#messages}}
	  		<div class="alert alert-danger">{{}}</div>
	  	{{/messages}}

	  	-->

	  	<!-- LOGIN FORM -->
      <div style="width:70%;margin:0 auto;">
        <h1><img src="img/logo2.png" height="100"/> sound4Cast</h1>
        <p>Login or Register with:</p>
	  	<form action="/login" method="post">
	  		<div class="form-group">
	  			<label>Email</label>
	  			<input type="text" class="form-control" name="email">
	  		</div>
	  		<div class="form-group">
	  			<label>Password</label>
	  			<input type="password" class="form-control" name="password">
	  		</div>

	  		<button type="submit" class="btn btn-primary btn-lg">Login</button>
	  	</form>

	  	<hr>

	  	<p>Need an account? <a href="/signup">Signup</a></p>
	  	<p>Or go <a href="/">home</a>.</p>
    </div>

	  </div>
  </div>
  </body>
</html>
